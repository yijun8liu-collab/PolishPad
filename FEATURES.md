# PolishPad 功能清单（回归测试基准）

> 基准版本：提交 `85edc4f`（v0.5 稳定基线 + API Key 改存配置文件）。
> **用法**：新增/修改功能前，先圈出可能受影响的模块；改完后对照逐条验证。
> 同一功能的不同入口（快捷键 / 菜单 / 按钮 / 右键服务）**各算一条**，分别测试。
> 自动化冒烟：`swift tools/verify_baseline.swift`（核心链路 F1→F22→F24，
> 需要机器空闲、不要同时操作键鼠）；单元自检：`PolishPad --selftest`（25 项）；
> 提示词回归：`python3 tools/prompt_test.py`（改提示词后必跑，检查输出是改写而非回答）。

## 一、全局入口

| 编号 | 功能 | 触发方式 | 预期效果 |
|---|---|---|---|
| F1 | 唤起主面板 | 全局热键（默认 `ctrl+option+p`，可配置） | 面板出现在**记忆位置**（用户拖过即记住；无记忆/显示器变化时回鼠标屏幕中上默认位）；**尺寸可调**；**全新会话**；**原应用有选中文本时自动带入草稿**（密码框除外、≤4000 字）；草稿框自动聚焦；记录唤起前的前台应用为粘贴目标 |
| F2 | 关闭主面板 | 面板开着时再按同一热键 | 面板关闭，焦点还给唤起前的应用；**进行中的请求同时取消**（与 Esc/红点一致） |
| F3 | 菜单栏菜单 | 点击菜单栏图标 | 弹出完整菜单（各项见 F4-F15）；**每次展开重建：文案跟随面板 中/EN 开关即时切换**，历史子菜单保持最新 |
| F4 | 打开优化窗口 | 菜单项（标题带当前热键） | 同 F1/F2 切换；注意：点菜单不会切走目标应用焦点 |
| F5 | 划词优化 | 全局热键（默认 `ctrl+option+r`），先选中文本 | HUD「优化中…」（流式字数进度）→ 优化结果**原地替换选中文本** → 恢复用户原剪贴板（含图片）→ Glass 音效 + HUD「已替换」；等修饰键物理松开才模拟 ⌘C（防叠加）；**结果返回时目标应用已不在前台 → 不盲贴，结果留剪贴板并提示手动粘贴**；**原文+结果写入历史（可从菜单找回被替换的原文）**；**场景 = 目标应用的感知映射 > 全局当前场景（F72）** |
| F6 | 划词优化 · 菜单入口 | 菜单「优化选中文本」 | 同 F5 |
| F7 | 划词优化 · 右键服务 | 目标应用中右键 → 服务 → 「PolishPad：优化并替换」 | 同 F5 |
| F8 | 全选优化 | 全局热键（默认 `ctrl+option+a`），光标在输入框内即可 | 先模拟 ⌘A 全选，其余同 F5；输入框为空时报错且不破坏原文 |
| F9 | 全选优化 · 菜单入口 | 菜单「全选优化替换」 | 同 F8 |
| F10 | 全选优化 · 右键服务 | 右键 → 服务 → 「PolishPad：全选优化并替换」 | 同 F8 |
| F11 | 还原上次替换 | 菜单「还原上次替换」 | 撤销最近一次 F5/F8 替换，恢复原文；**仅当目标应用仍在前台时执行**（防止按键误发到别的应用） |
| F12 | 历史 | 菜单「历史」子菜单 | 最近 20 条记录（**含面板会话和划词/全选优化**）；每条展开原文+各版本；点击任意版本复制到剪贴板 |
| F13 | 设置入口 · 菜单 | 菜单「设置…」 | 打开设置窗口（见模块五） |
| F14 | 打开配置文件 | 菜单「打开配置文件」 | 用默认编辑器打开 config.json |
| F15 | 退出 | 菜单「退出 PolishPad」（⌘Q） | 应用退出 |
| F73 | 新手引导 | 首次启动自动（key 未配或未授权时）；菜单栏「新手引导」可重看 | 三步向导：① 连接 AI（测试连接通过才可下一步，通过即存配置）② 授权辅助功能（一键直达系统设置 + 每秒轮询自动亮绿）③ 30 秒上手实战（教学模式：面板预填示例草稿、结果直接写入演示框不发合成按键，两任务完成划掉）；每步可跳过；已配置+已授权的老用户静默豁免；**第 3 步含开机自启动勾选**；红 X 关闭正确复位教学模式 |
| F74 | 静默更新提醒 | 启动时自动（每日 ≤1 次，零弹窗） | 后台对比最新 Release；有新版时菜单栏菜单**顶部**出现「升级到 vX.Y.Z…」，点击即走一键更新管线（HUD 进度），失败降级打开下载页 |
| F76 | 面板入场/退场动效 | 唤起/关闭时自动 | 入场：窗口 150ms 淡入（含投影）+ 内容 94%→100% 缩放与 4px→0 模糊聚焦（visionOS 式材质动效）；退场统一 120ms 淡出；快速再唤起不受进行中淡出影响 |
| F75 | 面板位置记忆（多屏感知） | 拖动面板自动记录 | 记忆的是**屏幕内相对位置**（如"横向 62%、纵向 78%"）；唤起时面板出现在**当前工作屏**（前台焦点窗口所在屏 → 鼠标屏 → 主屏兜底），并套用同一"位置感"——多屏切换零感知、换分辨率不出界（带边界夹紧）；旧绝对坐标自动迁移；⋯ 菜单「恢复默认位置」一键还原（2026-08-03 多屏改造） |
| F73a | 首次使用流程卡 | 面板空态自动（完成/跳过引导前） | 按使用顺序三步讲解（输入→回车自动贴回→补充/修改/关闭）；点「知道了」或完成引导后永久消失 |
| F72 | 全局场景 · 菜单栏切换 | 菜单第一层「场景：〈当前名〉」子菜单 | 内置 + 用户场景勾选切换；选择即全局生效并持久化（写 config.promptPreset）：划词/全选立即使用、面板下次唤起默认、设置的"场景"选择器同一份状态（**设置窗开着时也会同步，保存不会写回旧值**）；HUD 确认提示；应用感知映射优先于全局场景 |

## 二、主面板 · 组稿阶段

| 编号 | 功能 | 触发方式 | 预期效果 |
|---|---|---|---|
| F16 | 草稿输入 | 面板草稿框打字 | 多行输入；**Enter 提交、Shift+Enter 换行**；中文输入法组字中的回车交给输入法，不误提交 |
| F17 | Esc 分层退出 | Esc 键 | 录音中→仅停止录音；请求中→仅取消请求（恢复本轮前内容）；其他→关闭面板 |
| F18 | 语音听写 | 点击麦克风按钮 | 开始/停止录音；识别文字**追加**在框内已有文字之后（长停顿不丢前文）；识别语言固定跟设置 speechLocale（**不随 中/EN 开关变**——说中文出英文是核心场景） |
| F19 | 中/EN 切换 | 面板右上角 中/EN | 切换整个界面语言 + 输出语言（EN 用原生英文提示词，非中文提示词打补丁）；**菜单栏菜单、HUD、设置窗口（下次打开时）一并跟随**；选择被记住（重启保留） |
| F20 | 关闭按钮 | 面板**左上角红色圆点**（mac 风格，悬停显 ×） | 无条件收尾并关窗（停录音、取消请求） |
| F20a | 设置按钮 | 面板**右上角齿轮图标** | 打开设置窗口（与 ⋯ 菜单/菜单栏入口等效） |
| F21 | 场景预设 | 底栏预设按钮（润色/Slack英文/正式/精简/自定义） | 唤起时按前台应用自动选中（appPresets 映射，显示"已按 X 自动选择"）；手动点击可切换（**含用户自定义场景**）；审阅阶段切换会用新场景重新生成当前文本 |
| F22 | 提交优化 | 草稿框 Enter | 流式显示结果 → 结果自动复制到剪贴板 → **自动粘贴回唤起时的前台应用**（HUD「已粘贴」）→ 焦点回到面板反馈框；每轮写入历史 |

## 三、主面板 · 审阅阶段（多轮纠偏）

| 编号 | 功能 | 触发方式 | 预期效果 |
|---|---|---|---|
| F23 | 追加轮次 | 追加模式（默认）下输入新内容，Enter | 新内容优化后由模型**并入全文合适位置**，输出完整全文；自动替换上一次粘贴（先按上次粘贴长度退格，再贴，HUD「已替换」） |
| F24 | 修改轮次 | 修改模式下输入修改意见，Enter | 输出按意见修改后的完整全文；替换逻辑同 F23 |
| F25 | 模式切换 · 点击 | 点击「追加/修改」标签 | 切换反馈语义；输入框占位文字随之变化 |
| F26 | 模式切换 · Tab | 反馈框内按 Tab | 同 F25 |
| F27a | 快捷 chip「更短」 | 点击 chip | 按**修改**语义发送对应固定意见，其余同 F24 |
| F27b | 快捷 chip「更正式」 | 点击 chip | 同 F27a |
| F27c | 快捷 chip「更口语」 | 点击 chip | 同 F27a |
| F27d | 快捷 chip「展开」 | 点击 chip | 同 F27a |
| F28 | 满意收工 | 反馈框空着直接 Enter | 已自动粘贴过→仅关窗，但**若 ⌘[ 回退过版本，先把目标内容原地替换为当前显示版本再关窗**；未粘贴过或粘贴曾失败→关窗并粘贴到原应用 |
| F29 | 版本切换 | ⌘[ 上一版 / ⌘] 下一版 | 结果区显示对应版本并复制到剪贴板；回退后发反馈以**当前显示的版本**为基准 |
| F30 | 改动对比 | 点击状态行右侧的强度刻度 | 与上一版的字符级 diff：新增绿底、删除红字划线；再点一次还原；超长文本自动跳过（2026-07-29 界面简化：独立「改动」按钮并入强度刻度） |
| F31 | 复制原文 | 按钮「复制原文」 | 复制未优化的原始草稿 |
| F32 | 手动粘贴回 | … 菜单「粘贴并替换（↩，留空完成）」 | 关窗并粘贴，同 F28 后半；… 菜单审阅态还提供 再次复制 / 重新生成 / 上一版 / 下一版 |
| F33 | 再次复制 | 按钮「再次复制」 | 当前显示版本重新复制到剪贴板 |
| F34 | 重新开始 | ⌘N 或按钮「重新开始」 | 清空会话回到组稿阶段（版本、对话、粘贴状态全清） |
| F35 | 截断警告 | 自动（新版字数 < 上一版 30%） | 状态栏提示"比上一版短很多，请检查是否完整"，不拦截 |
| F36 | 失败处理 | API 报错/网络失败 | 显示错误信息；保留上一版结果；剪贴板内容不丢（状态栏提示 vN 仍在剪贴板） |
| F37 | 中途取消 | 流式期间 Esc | 取消请求，结果区恢复本轮开始前的版本 |
| F38 | 面板内编辑快捷键 | 面板内 ⌘V/⌘C/⌘X/⌘A/⌘Z | 正常可用（编辑菜单已注册） |
| F77 | 改动强度刻度 | 每轮结果生成后自动 | 状态行**右侧**五格胶囊 + 标签（轻润色/小修改/中等改写/较大改写/大幅重写），按新旧文本字符相似度分档（≥0.9/≥0.75/≥0.55/≥0.35/其余）；≤2 格绿、3 格琥珀、≥4 格橙；**刻度本身是按钮，点击 = 改动对比（F30）**；新一轮开始时清空 |
| F78 | 场景色 | 切换场景自动 | 场景 chip 胶囊底色、面板描边渐变下端、蜕变动画光标统一随当前场景着色：优化蓝 / Slack 紫 / 正式钢青 / 精简琥珀；自建场景按 id 从 5 色盘稳定取色（同一场景颜色恒定） |
| F79 | 版本圆点时间线 | 首版结果出现即常驻 | 状态行左侧一排小圆点（v 数字徽章已移除，圆点即版本指示），当前版本用场景色并放大；hover 预览该版开头 24 字并附 ⌘[ ⌘] 提示；点击直达任意版本（复制到剪贴板）；命中区固定 14×14；流式期间禁用。结果成功状态收敛为绿勾图标，4 秒自动淡出（悬停看详情；截断警告文字仍常驻） |
| F80 | 智能建议 chips | 每轮结果就绪后后台自动 | 额外发一次轻量请求生成 2–3 条 ≤20 字的针对性修改建议，**替换** F27a–d 固定四枚；点击即按修改语义发送该建议；生成/解析失败或换轮开始时静默回退固定 chips，不报错 |
| F82 | 重新生成 | 状态栏 arrow.clockwise 按钮或 ⌘R（有结果且非流式时） | 用产生上一轮结果的同一请求重跑一遍，结果**追加**为新版本（不覆盖）；系统消息按当前语言/场景/**语气调音台**重建——调完语气按 ⌘R 即以新语气重出 |
| F81 | 语气调音台 | 底栏 slider 图标按钮 | 弹层两根滑杆：口语↔正式、精简↔详尽（0–100，默认 50）；偏离中点 >2 时注入系统提示影响**下一轮**生成，按钮以场景色高亮并出现"恢复默认"；新会话/⌘N 恢复默认 |

## 四、粘贴与替换机制（隐式行为）

| 编号 | 行为 | 说明 |
|---|---|---|
| F39 | 粘贴目标锁定 | 目标 = **唤起面板时的前台应用**（无切换跟踪）；粘贴前轮询确认应用真的回到前台（最多 2s），失败则 HUD「已复制，请手动粘贴」且**回滚"已粘贴"标记**（空回车恢复粘贴语义），绝不盲贴 |
| F40 | 原地替换方式 | 按上一次粘贴文本的字符数发退格删除，再 ⌘V（不用 ⌘Z——终端类应用不支持文本撤销）；退格分批发送防事件洪峰 |
| F41 | 剪贴板纪律 | 面板每轮结果留在剪贴板（设计如此）；划词/全选优化用完**恢复**用户原剪贴板（含图片等非文本） |
| F42 | 替换可还原 | 每次 F5/F8 替换都记录（替换前后文本+目标），供 F11 还原 |

## 五、设置窗口

| 编号 | 功能 | 触发方式 | 预期效果 |
|---|---|---|---|
| F43 | API 配置 | 设置窗口填写 | Base URL / API Key / 模型 / temperature / maxTokens；**Key 明文存 config.json（0600 权限），不使用钥匙串，无系统弹窗** |
| F44 | 测试连接 | 按钮 | 用当前填写值真实调用一次 API，显示成败 |
| F45 | 快捷键设置 | 三个热键字段 | 支持点击字段后**直接按组合键录制**，也可手输；保存时格式校验（非法格式报错不保存）；保存后热键**即时重载**，无需重启；录制中按住修饰键实时回显、拒绝时显示原因；**关窗自动停止录制并恢复热键** |
| F46 | 场景与提示词 | 预设下拉 + 提示词编辑器 | **内置场景的提示词全文公开可查看/可直接修改**（修改即成为覆写版，中/EN 共用，"已自定义"角标 + 一键恢复默认；空值/与内置一致不落盘）；**旧"自定义"预设已移除**：启动时自动迁移为一个名为"自定义"的用户场景（选中/映射引用同步改指向） |
| F47 | 应用感知映射 | 映射表（bundle id → 预设） | 增删行；对应应用唤起面板时自动选中预设 |
| F48 | 术语表 | 多行文本（`术语=译法` 或 `术语`） | 注入提示词：翻译/改写时固定译法或原样保留 |
| F49 | 语音语言 | 下拉 | 听写识别语言 |
| F70 | 自定义场景 · 多个可命名 | 设置 → 场景预设 → ＋新建场景 | 可创建任意多个自定义场景，各自命名 + **中/EN 双提示词**（分段切换编辑；EN 版留空时自动用中文版+英文输出要求；中文版留空按内置优化处理）；可改名/删除（删除后默认场景回退"优化"，面板同步回退）；出现在**面板场景菜单**和**应用感知映射**中；存 config.json customScenarios |
| F71 | AI 生成场景 · 面板入口 | 场景菜单底部「✨ 描述创建新场景…」 | 面板中央弹出创建卡片：一句话描述 → AI **一次生成中英两版提示词正文**（多轮标签协议按语言本地附加，AI 不碰协议段；解析容忍代码围栏/杂讯/字符串内裸换行）→ 立即持久化并选用；失败显示错误、可重试 |
| F71a | AI 生成场景 · 设置入口 | 设置 → 场景预设 →「✨ AI 生成…」 | 弹窗输入描述，生成后自动选中新场景并提示；与 F71 同一生成管线 |
| F49a | 界面与输出语言 · 设置入口 | 设置 → 行为 → 中文/English 分段控件 | 与面板右上角 中/EN 开关同一份状态、双向同步；**即改即生效**（设置窗口自身文案同步翻转，不需保存） |
| F50 | 自动粘贴开关 | 开关 | 关闭后每轮只复制不粘贴，审阅界面出现「粘贴回原应用」按钮 |
| F51 | 开机自启动 | 开关 | SMAppService 注册/注销 |
| F52 | 本月用量 | 自动显示 | 请求次数 + 输入/输出 tokens（按月累计） |
| F53 | 检查更新 · 一键安装 | 设置 → 检查更新 → 下载并安装 | 对比 GitHub releases；有新版时一键完成 下载→版本校验→去隔离→原地替换→自动重启；Translocation/目录只读时降级为打开下载页；隐藏钩子 `--test-selfupdate` 供端到端回归 |
| F54 | 保存 | 保存按钮 | 写入 config.json（0600）+ 发通知热键重载 + **保存成功自动关窗**（HUD 提示"设置已保存"）；失败留在窗口显示错误 |

## 六、配置与数据

| 编号 | 行为 | 说明 |
|---|---|---|
| F55 | 配置文件 | `~/Library/Application Support/PolishPad/config.json`；**每次请求重新读取**（手改配置即时生效，无需重启）；首次运行自动生成模板 |
| F56 | 钥匙串反向迁移 | 仅当配置里是旧哨兵 `(stored-in-keychain)` 时，启动时读一次钥匙串把 key 搬回文件；平时零钥匙串接触 |
| F57 | 历史持久化 | 最近 20 条存 history.json（面板会话逐轮更新；划词/全选优化每次一条），应用重启不丢 |
| F58 | 用量持久化 | 按月存 UserDefaults |
| F59 | 自检模式 | `PolishPad --selftest`：25 项检查（配置/预设/术语/历史/diff/钥匙串往返/版本比较/用量） |

## 七、通用反馈

| 编号 | 行为 | 说明 |
|---|---|---|
| F60 | HUD 气泡 | 优化中…（含流式字数）/ 已粘贴 / 已替换 / 已复制（含降级提示） |
| F61 | 音效 | 替换成功 Glass；失败 Basso |
| F62 | 菜单栏图标状态 | 划词/全选优化期间图标变化（working → success → 恢复） |
| F63 | 主题切换 | … 菜单「切换为明亮/暗色主题」（2026-07-29 起收纳，底栏按钮移除） | 暗色玻璃 ⇄ 明亮通透玻璃；面板/HUD 气泡/设置窗口一起跟随；选择记住（UserDefaults），重启保留 |
| F64 | 面板大小 · 拖拽 | 拖动面板边缘/角 | 自由调整（最小 440×280，窄到放不下时快捷键提示自动隐藏），松手即记忆，下次唤起沿用 |
| F65 | 面板大小 · 预设 | 设置 → 行为 → 面板大小（小/中/大） | 小 560×340 / 中 680×400 / 大 820×500；保存后立即生效（面板可见时锚定顶边动画过渡）；手动拖过则显示"自定义"档 |
| F66 | 等待反馈 · 原地逐字蜕变 | 回车提交后自动（黑白模式通用） | 立即切审阅态：**旧文字（首轮=草稿，纠偏轮=上一版）整段飘舞**；流式开始后**新文字从左往右逐字定稿（聚焦入场），交界处旧字翻转消散**，蜕变节奏=生成节奏；新旧合计 >700 字回退为普通流式+▍ 光标；首字 5 秒未到状态栏显示"网络较慢，仍在等待…（Esc 取消）" |
| F69 | 结果快速编辑 | 审阅区直接点击输入 | 结果文本可就地修改（流式/录音期间锁定）；**回车=普通换行**不触发提交；编辑即生效——"再次复制"/空回车收工/后续修改轮自然以编辑后文本为准；切换版本会覆盖未另存的编辑（刻意不加持久化） |
| F68 | ~~神经脉冲背景~~ | — | **已移除**（2026-07-24，节省 CPU）：等待动画统一为 F66 逐字蜕变，面板无常驻动画层 |
| F67 | 停顿预取 | 组稿停止输入 ≥2s 且内容 ≥8 字时自动 | 后台静默预跑一轮并缓存；回车时输入与提示词（场景/语言/术语表）完全未变则秒出缓存结果（状态栏带闪电图标），任何改动自动作废缓存走正常轮次；⌘N/关窗清空缓存；**听写录音中不预取**；**设置 → 行为 可关闭（默认开，含 token 消耗警告说明）** |
| F87 | 按住说话（无面板语音直达） | 设置 → 行为 → "按住说话"选热键（右⌘/右⇧/右⌃，默认关） | **对讲模式**：按住 ≥0.35s 说话、松开即走当前场景（含应用感知）LLM 优化并粘贴到焦点光标；**锁定模式**：短按 <0.35s 开始录音、再按一次结束、Esc 取消（上限 3 分钟）；HUD 实时滚动字幕（听写引擎与面板一致：Whisper 融合/系统）；原始转写自动入历史；优化失败时原始转写留在剪贴板；窗口切走不盲贴改为复制提示；面板开着时热键不响应 |
| F86 | 云端实时字幕引擎（讯飞） | 设置 → 行为 → "实时字幕引擎"选"讯飞云端"（默认系统识别；选中自动展开 APPID/APIKey/APISecret 配置） | Whisper 融合模式的实时层改走讯飞：**大模型（中文识别大模型）→ 额度不足/失败自动降级经典版（语音听写）→ 仍失败回退系统 SFSpeech** 三级链；按句开 WebSocket 会话（复用 silero VAD 边界，避开 60s 上限与会话缝隙丢字）；降级时当前句音频原样重发不丢字；会话终稿自动升级停顿快照；实测术语实时还原 ≈82%（系统引擎明显更弱）；语音上云、按量计费需用户知情；--test-xfyun <wav> 静默验证钩子 |
| F85 | 语音误识自动纠正 | 所有场景统一注入（systemContent 层） | 润色时自动识别并还原语音识别的同音/近音误识：不合常理的音译汉字按发音还原为本意（技术语境优先英文术语，如「白塞子→batch size」「书路→输入」「楼斯→loss」）；只纠明显误识、不确定保留；实测连未教过的「欧提麦泽→optimizer」也能推理还原，干净文本不误纠 |
| F84 | Whisper 大模型语音识别 | 设置 → 行为 → "Whisper 大模型语音识别（本地）"（默认关） | 开启即自动下载模型（约 874MB，hf-mirror 优先、huggingface 兜底，带进度/取消/重试）；听写改用 whisper.cpp large-v3-turbo（Metal 加速、全程本地）：中英混合准确率远超系统引擎；能量 VAD 按停顿分段；**双引擎融合**：系统识别负责当前句逐字实时上屏（丝滑），Whisper 每句停顿后高质量定稿**原地替换**；系统侧文字兼当安全网（Whisper 闸门误丢时不丢话）；系统识别不可用时退回滑动窗口预览（约每秒更新）；术语表自动注入识别提示词；模型未就绪时自动回退系统引擎并提示；录音结束释放模型内存 |
| F83 | 默认开启语音 | 设置 → 行为 → "打开面板自动开启语音输入"（默认关） | 开启后每次唤起面板即自动进入听写（等同点击麦克风），说完直接回车；教学模式不触发；Esc/关窗即停；识别语言跟随语音识别语言设置 |

## 已知边界（当前版本刻意不支持）

- 面板开着时点击别的应用/输入框，粘贴目标**不会**跟着走——目标永远是唤起时的前台应用。换目标 = 关面板重新唤起。（切换跟踪功能已于 2026-07-22 整体放弃，历史见 git `d15b047..fcbc878`）
- 每次唤起都是全新会话，无逐输入框会话记忆。
- 替换按"上次粘贴长度退格"，若用户在目标输入框手动改过已粘贴的内容，替换可能删错位置。
